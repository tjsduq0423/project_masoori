import { Animated } from "react-native";
import { ThumbButton, ThumbChildrenPlacement, TrackBar } from "./types";
export declare const useTitleTextColor: (toggleValue: boolean, activeColor: string, inActiveColor: string, title: string | null, placement: ThumbChildrenPlacement) => {
    textColor: string;
} | null;
export declare const useToggleValue: (value: boolean, thumbButton: ThumbButton, trackBar: TrackBar, animationDuration: number, onPress: (val?: boolean) => void) => {
    toggleValue: boolean;
    handlePress: () => void;
    handleLongPress: () => void;
    fadeAnim: Animated.Value;
};
